---
phase: 02-mayhem-serial-interface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hackrf_ros_interfaces/package.xml
  - hackrf_ros_interfaces/CMakeLists.txt
  - hackrf_ros_interfaces/srv/AppStart.srv
  - hackrf_ros_interfaces/srv/SetFreq.srv
autonomous: true
requirements:
  - MAY-03
  - MAY-04

must_haves:
  truths:
    - "ros2 interface show hackrf_ros_interfaces/srv/AppStart returns a schema with string app_name request and bool success + string message response"
    - "ros2 interface show hackrf_ros_interfaces/srv/SetFreq returns a schema with int64 freq_hz request and bool success + string message + int64 confirmed_hz response"
    - "colcon build succeeds for hackrf_ros_interfaces with no errors"
  artifacts:
    - path: "hackrf_ros_interfaces/srv/AppStart.srv"
      provides: "Service contract for switching Mayhem apps by name"
      contains: "string app_name"
    - path: "hackrf_ros_interfaces/srv/SetFreq.srv"
      provides: "Service contract for setting radio frequency with confirmation"
      contains: "int64 freq_hz"
    - path: "hackrf_ros_interfaces/CMakeLists.txt"
      provides: "rosidl_generate_interfaces build target"
      contains: "rosidl_generate_interfaces"
    - path: "hackrf_ros_interfaces/package.xml"
      provides: "ROS2 CMake package manifest"
      contains: "rosidl_default_generators"
  key_links:
    - from: "hackrf_ros_interfaces/CMakeLists.txt"
      to: "hackrf_ros_interfaces/srv/AppStart.srv"
      via: "rosidl_generate_interfaces()"
      pattern: "AppStart\\.srv"
    - from: "hackrf_ros_interfaces/CMakeLists.txt"
      to: "hackrf_ros_interfaces/srv/SetFreq.srv"
      via: "rosidl_generate_interfaces()"
      pattern: "SetFreq\\.srv"
---

<objective>
Create the `hackrf_ros_interfaces` ROS2 CMake package containing the custom service definitions that Plan 03 imports for the appstart and setfreq ROS2 services.

Purpose: Python packages cannot define `.srv` files without a parallel CMake package. This interfaces package is the standard ROS2 pattern and the prerequisite for Plan 03's service registrations.
Output: A buildable `hackrf_ros_interfaces` package at `/home/user/dev_ws/hackrf_ros_interfaces/` with two compiled service types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/dev_ws/hackrf_ros/.planning/PROJECT.md
@/home/user/dev_ws/hackrf_ros/.planning/ROADMAP.md
@/home/user/dev_ws/hackrf_ros/.planning/STATE.md

<interfaces>
<!-- Service schemas to implement verbatim. Source: 02-RESEARCH.md Pattern 4. -->

AppStart.srv:
```
string app_name
---
bool success
string message
```

SetFreq.srv:
```
int64 freq_hz
---
bool success
string message
int64 confirmed_hz
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hackrf_ros_interfaces CMake package with srv files</name>
  <files>
    /home/user/dev_ws/hackrf_ros_interfaces/package.xml
    /home/user/dev_ws/hackrf_ros_interfaces/CMakeLists.txt
    /home/user/dev_ws/hackrf_ros_interfaces/srv/AppStart.srv
    /home/user/dev_ws/hackrf_ros_interfaces/srv/SetFreq.srv
  </files>
  <read_first>
    - /home/user/dev_ws/hackrf_ros/.planning/phases/02-mayhem-serial-interface/02-RESEARCH.md (Pattern 4: custom srv path)
    - /home/user/dev_ws/hackrf_ros/package.xml (version and format reference)
  </read_first>
  <action>
Create directory `/home/user/dev_ws/hackrf_ros_interfaces/srv/` and write the four files below. Do NOT create inside `hackrf_ros/` — this is a sibling package at `/home/user/dev_ws/hackrf_ros_interfaces/`.

**`/home/user/dev_ws/hackrf_ros_interfaces/srv/AppStart.srv`** (exact content):
```
string app_name
---
bool success
string message
```

**`/home/user/dev_ws/hackrf_ros_interfaces/srv/SetFreq.srv`** (exact content):
```
int64 freq_hz
---
bool success
string message
int64 confirmed_hz
```

**`/home/user/dev_ws/hackrf_ros_interfaces/package.xml`**:
```xml
<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>hackrf_ros_interfaces</name>
  <version>0.0.0</version>
  <description>Custom service definitions for hackrf_ros Mayhem serial control</description>
  <maintainer email="root@todo.todo">root</maintainer>
  <license>MIT</license>

  <buildtool_depend>ament_cmake</buildtool_depend>
  <buildtool_depend>rosidl_default_generators</buildtool_depend>

  <exec_depend>rosidl_default_runtime</exec_depend>
  <exec_depend>std_msgs</exec_depend>

  <member_of_group>rosidl_interface_packages</member_of_group>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
```

**`/home/user/dev_ws/hackrf_ros_interfaces/CMakeLists.txt`**:
```cmake
cmake_minimum_required(VERSION 3.8)
project(hackrf_ros_interfaces)

find_package(ament_cmake REQUIRED)
find_package(rosidl_default_generators REQUIRED)
find_package(std_msgs REQUIRED)

rosidl_generate_interfaces(${PROJECT_NAME}
  "srv/AppStart.srv"
  "srv/SetFreq.srv"
  DEPENDENCIES std_msgs
)

ament_export_dependencies(rosidl_default_runtime)
ament_package()
```
  </action>
  <verify>
    <automated>
cd /home/user/dev_ws && colcon build --packages-select hackrf_ros_interfaces 2>&1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    - `colcon build --packages-select hackrf_ros_interfaces` exits 0 with "1 package finished"
    - `grep -r "string app_name" /home/user/dev_ws/hackrf_ros_interfaces/srv/AppStart.srv` returns a match
    - `grep -r "int64 freq_hz" /home/user/dev_ws/hackrf_ros_interfaces/srv/SetFreq.srv` returns a match
    - `grep "rosidl_generate_interfaces" /home/user/dev_ws/hackrf_ros_interfaces/CMakeLists.txt` returns a match
    - `grep "int64 confirmed_hz" /home/user/dev_ws/hackrf_ros_interfaces/srv/SetFreq.srv` returns a match (three-field response)
  </acceptance_criteria>
  <done>hackrf_ros_interfaces builds cleanly; AppStart.srv and SetFreq.srv are compiled ROS2 service types ready for import in Plan 03.</done>
</task>

<task type="auto">
  <name>Task 2: Source install and verify service introspection</name>
  <files>
    (no new files — verification step)
  </files>
  <read_first>
    (none — previous task output only)
  </read_first>
  <action>
Source the install overlay and verify the generated types are introspectable via the ROS2 CLI. Run the verification commands below. If `ros2 interface show` fails, check that `source /home/user/dev_ws/install/setup.bash` was run after colcon build. If the package was not found, confirm `hackrf_ros_interfaces` appears in `colcon build` output.

No files to modify — this task confirms the build output is correct before Plan 03 depends on it.
  </action>
  <verify>
    <automated>
source /home/user/dev_ws/install/setup.bash 2>/dev/null; ros2 interface show hackrf_ros_interfaces/srv/AppStart && ros2 interface show hackrf_ros_interfaces/srv/SetFreq
    </automated>
  </verify>
  <acceptance_criteria>
    - `ros2 interface show hackrf_ros_interfaces/srv/AppStart` prints `string app_name` in the request section and `bool success` + `string message` in the response section
    - `ros2 interface show hackrf_ros_interfaces/srv/SetFreq` prints `int64 freq_hz` in request and `bool success` + `string message` + `int64 confirmed_hz` in response
    - No "Unknown interface type" or "Package not found" errors
  </acceptance_criteria>
  <done>Both service types are introspectable. Plan 03 can safely import `from hackrf_ros_interfaces.srv import AppStart, SetFreq`.</done>
</task>

</tasks>

<verification>
After both tasks:
- `ls /home/user/dev_ws/hackrf_ros_interfaces/srv/` shows `AppStart.srv` and `SetFreq.srv`
- `colcon build --packages-select hackrf_ros_interfaces` succeeds
- `ros2 interface show hackrf_ros_interfaces/srv/AppStart` returns correct schema
- `ros2 interface show hackrf_ros_interfaces/srv/SetFreq` returns correct schema with `confirmed_hz`
</verification>

<success_criteria>
The `hackrf_ros_interfaces` package builds and the two service types are available as compiled Python bindings. A downstream node can `from hackrf_ros_interfaces.srv import AppStart, SetFreq` without ImportError.
</success_criteria>

<output>
After completion, create `/home/user/dev_ws/hackrf_ros/.planning/phases/02-mayhem-serial-interface/02-01-SUMMARY.md`
</output>
