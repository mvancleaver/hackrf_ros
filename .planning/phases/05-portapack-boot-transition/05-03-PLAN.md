---
phase: 05-portapack-boot-transition
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [REQ-P5-00, REQ-P5-01, REQ-P5-02, REQ-P5-03, REQ-P5-04, REQ-P5-05, REQ-P5-06, REQ-P5-07, REQ-P5-08, REQ-P5-09, REQ-P5-10, REQ-P5-11, REQ-P5-12, REQ-P5-13, REQ-P5-14, REQ-P5-15, REQ-P5-16, REQ-P5-A1, REQ-P5-A3]
tags: [docs, scope-reversal, requirements, project-mgmt]
must_haves:
  truths:
    - "PROJECT.md 'Out of Scope' section no longer reads the unconditional 'Mayhem firmware serial control — separate package (pymayhem)' line"
    - "PROJECT.md 'Out of Scope' instead reads a bounded statement that Mayhem UI navigation, DFU, file transfer, and TX apps are out of scope BUT the single Mayhem→HackRF mode-switch command is in-package"
    - "REQUIREMENTS.md 'Out of Scope' table row for 'Mayhem firmware control' is replaced with the bounded variant"
    - "REQUIREMENTS.md defines a new 'Phase 5 — Portapack Boot Transition' section declaring REQ-P5-00 through REQ-P5-16 plus REQ-P5-A1 and REQ-P5-A3"
    - "REQUIREMENTS.md Traceability table lists each REQ-P5-NN row with Phase 5 (Portapack Boot Transition) and status Pending"
    - "REQUIREMENTS.md coverage counts updated to reflect the new Phase 5 requirement IDs"
    - "Each new REQ-P5-NN entry cites the corresponding D-XX decision from 05-CONTEXT.md (or assumption A1/A3 from 05-RESEARCH.md)"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Bounded scope statement acknowledging the mode-switch is in-package (D-00 reversal)"
      contains: "Mayhem mode-switch command"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 5 REQ-P5-00..REQ-P5-16, REQ-P5-A1, REQ-P5-A3 with D-XX mapping + updated traceability + rewritten Out of Scope row"
      contains: "REQ-P5-00"
  key_links:
    - from: ".planning/PROJECT.md Out of Scope"
      to: ".planning/REQUIREMENTS.md Phase 5 section"
      via: "bounded scope statement cross-reference"
      pattern: "mode-switch"
    - from: ".planning/REQUIREMENTS.md Phase 5 section"
      to: ".planning/REQUIREMENTS.md Traceability table"
      via: "REQ-P5-NN IDs referenced in both places"
      pattern: "REQ-P5-[0-9]+"
    - from: "05-CONTEXT.md D-XX decisions"
      to: "REQUIREMENTS.md REQ-P5-NN"
      via: "1:1 mapping recorded in each REQ-P5-NN description"
      pattern: "D-[0-9]+"
---

<objective>
Reverse the bounded scope decision locked in CONTEXT.md D-00 and mint first-class requirement IDs for Phase 5. This plan only edits `.planning/PROJECT.md` and `.planning/REQUIREMENTS.md` — no code changes, no deployment changes. It produces the REQ-P5-NN IDs that all other Phase 5 plans (05-01, 05-02, 05-04) reference in their `requirements` frontmatter.

Purpose: Before this plan runs, REQUIREMENTS.md has no Phase 5 coverage and PROJECT.md explicitly rules out the capability this phase delivers. After this plan runs, the project's scope documents accurately describe what the driver will do after 05-02 lands, and traceability is preserved.

Output: Two modified planning documents. No runtime artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-portapack-boot-transition/05-CONTEXT.md
@.planning/phases/05-portapack-boot-transition/05-RESEARCH.md

<interfaces>
<!-- The exact existing text being replaced — captured from HEAD 2026-04-18. -->

# PROJECT.md line 45 (current):
#   - Mayhem firmware serial control — separate package (pymayhem)
# This sits inside the "### Out of Scope" section under "## Requirements".

# REQUIREMENTS.md line 88 (current):
#   | Mayhem firmware control | Separate package concern (pymayhem) |
# This sits inside the "## Out of Scope" table ("| Feature | Reason |").

# REQUIREMENTS.md Phase distribution block (lines 131-136):
#   **Coverage:**
#   - v1 requirements: 32 total
#   - Mapped to phases: 32
#   - Unmapped: 0
#   **Phase distribution:**
#   - Phase 1 (Sensor Foundations): 14 requirements (MSG-01..06, DET-01..06, PERF-01..02)
#   - Phase 2 (Robot Autonomy Integration): 10 requirements (SWP-01..03, REC-01..04, MAP-01..03)
#   - Phase 3 (Reliability): 3 requirements (REL-01..03)
#   - Phase 4 (Advanced Signal Intelligence): 5 requirements (ADV-01..03, HW-01..02)

# REQUIREMENTS.md Traceability table ends at line 125 (ADV-03 row);
# HW-01 and HW-02 are appended above. New Phase 5 rows go AFTER HW-02.

# D-XX decisions in 05-CONTEXT.md (authoritative source of REQ-P5-NN mapping):
#   D-00: Scope reversal (project-level meta-decision)
#   D-01: Udev VID:PID match
#   D-02: Symlink name /dev/portapack
#   D-03: docker-compose cgroup rules + /dev bind
#   D-04: Rule installed to host /etc/udev/rules.d/
#   D-05: pyserial 115200 8N1
#   D-06: open→write→close→poll→open sequence
#   D-07: Symlink presence gate (skip path)
#   D-08: Serial-open failure → log ERROR, fall through
#   D-09: Resend path + 2-attempt FAILED terminal
#   D-10: pyhackrf2 open retry loop (250 ms × portapack_open_retries)
#   D-11: Diagnostics last_portapack_transition
#   D-12: Four ROS params (device, enable, timeout, retries)
#   D-13: Module constants
#   D-14: Params NOT dynamic (configure-time only)
#   D-15: Diagnostics field format
#   D-16: Integration point in on_configure; helper returns enum

# Assumptions from 05-RESEARCH.md requiring hardware verification:
#   A1: Portapack CDC-ACM VID:PID = 1d50:6018  -> REQ-P5-A1
#   A3: 50 ms DTR settle sufficient             -> REQ-P5-A3
#   (A2 rolls into REQ-P5-05 since the command terminator is implicit in
#    the D-05 binding of b'hackrf\n'; no separate REQ-P5-A2.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reverse PROJECT.md line 45 Out of Scope statement</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (lines 42-50 — the "Out of Scope" block)
    - .planning/phases/05-portapack-boot-transition/05-CONTEXT.md (D-00 — exact language to use)
  </read_first>
  <action>
    Edit `.planning/PROJECT.md`. Replace the single line:
    ```
    - Mayhem firmware serial control — separate package (pymayhem)
    ```
    with these two lines (preserve the leading `- ` bullet format, keep the trailing TX/Redis/GUI bullets unchanged):
    ```
    - Mayhem mode-switch command (exit Mayhem UI → HackRF USB-SDR) — in-package since Phase 5 (bounded scope per CONTEXT.md D-00)
    - All other Mayhem firmware control (UI navigation, app launch, DFU, file transfer, TX apps) — still separate package concern (pymayhem)
    ```

    Do NOT remove or modify:
    - The section header `### Out of Scope`
    - The other bullets in this section (TX transmission, Redis IQ streaming, GUI applications)
    - Any other line in the file

    This implements D-00's bounded reversal: one command in, everything else still out.
  </action>
  <verify>
    <automated>! grep -q "Mayhem firmware serial control — separate package (pymayhem)" .planning/PROJECT.md &amp;&amp; grep -q "Mayhem mode-switch command" .planning/PROJECT.md &amp;&amp; grep -q "bounded scope per CONTEXT.md D-00" .planning/PROJECT.md &amp;&amp; grep -q "All other Mayhem firmware control" .planning/PROJECT.md &amp;&amp; grep -q "^### Out of Scope" .planning/PROJECT.md &amp;&amp; grep -q "TX transmission" .planning/PROJECT.md &amp;&amp; grep -q "Redis IQ streaming" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` does NOT contain the old string `Mayhem firmware serial control — separate package (pymayhem)`
    - `.planning/PROJECT.md` contains `Mayhem mode-switch command` (new bullet)
    - `.planning/PROJECT.md` contains `bounded scope per CONTEXT.md D-00` (explicit decision cite)
    - `.planning/PROJECT.md` contains `All other Mayhem firmware control` (boundary-drawing bullet)
    - `.planning/PROJECT.md` still contains the `### Out of Scope` heading
    - The TX transmission, Redis, and GUI bullets under Out of Scope are unchanged
  </acceptance_criteria>
  <done>
    PROJECT.md reflects the bounded reversal: mode-switch in, other Mayhem still out.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reverse REQUIREMENTS.md Out of Scope row for Mayhem firmware control</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 75-90 — Out of Scope table)
    - .planning/phases/05-portapack-boot-transition/05-CONTEXT.md (D-00)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md`. In the "## Out of Scope" table (the `| Feature | Reason |` one), replace the single row:
    ```
    | Mayhem firmware control | Separate package concern (pymayhem) |
    ```
    with these two rows (preserve Markdown table syntax — pipes, alignment, trailing newline):
    ```
    | Mayhem mode-switch command (exit Mayhem UI → HackRF USB-SDR) | IN SCOPE since Phase 5 (Portapack Boot Transition). Bounded reversal of prior decision per CONTEXT.md D-00. See REQ-P5-00. |
    | All other Mayhem firmware control (UI nav, DFU, file transfer, TX apps) | Separate package concern (pymayhem). Phase 5 narrowly excepts the mode-switch only. |
    ```

    Do NOT modify other rows (`TX transmission`, `Protocol demodulation`, `ML-based AMC`, `DSSS/FHSS signal tracking`, `Redis IQ streaming`, `GUI applications in core`, `Real-time signal decode`).
  </action>
  <verify>
    <automated>! grep -qE "^\| Mayhem firmware control \| Separate package concern \(pymayhem\) \|$" .planning/REQUIREMENTS.md &amp;&amp; grep -q "Mayhem mode-switch command" .planning/REQUIREMENTS.md &amp;&amp; grep -q "IN SCOPE since Phase 5" .planning/REQUIREMENTS.md &amp;&amp; grep -q "All other Mayhem firmware control" .planning/REQUIREMENTS.md &amp;&amp; grep -q "^| TX transmission" .planning/REQUIREMENTS.md &amp;&amp; grep -q "^| Redis IQ streaming" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` does NOT contain the old row `| Mayhem firmware control | Separate package concern (pymayhem) |`
    - `.planning/REQUIREMENTS.md` contains `Mayhem mode-switch command` as a table row
    - `.planning/REQUIREMENTS.md` contains `IN SCOPE since Phase 5` in the new row's Reason cell
    - `.planning/REQUIREMENTS.md` contains `All other Mayhem firmware control` as a follow-up row
    - The `| TX transmission |` row still exists unchanged
    - The `| Redis IQ streaming |` row still exists unchanged
    - Table syntax remains valid Markdown (no leading/trailing pipe omissions)
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md Out of Scope table mirrors PROJECT.md: mode-switch is in, everything else is out.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mint REQ-P5-00..REQ-P5-16 plus REQ-P5-A1, REQ-P5-A3 and add traceability rows</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 60-140 — v1 section, Out of Scope table, Traceability table, Coverage block)
    - .planning/phases/05-portapack-boot-transition/05-CONTEXT.md (complete — every D-XX)
    - .planning/phases/05-portapack-boot-transition/05-RESEARCH.md (§Assumptions Log for A1, A3)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md`. Three additions in this single task:

    **A. Add a new section header + requirement block after the existing "### Hardware Expansion" block** (which ends with HW-02 around line 65) and BEFORE `## v2 Requirements` (line 67). Insert exactly:

    ```markdown
    ### Portapack Boot Transition (Phase 5)

    Phase 5 minted these IDs during planning. Each maps 1:1 to a CONTEXT.md D-XX decision
    or a RESEARCH.md assumption requiring hardware verification. All Pending on creation.

    - [ ] **REQ-P5-00**: Scope-decision reversal — this REQUIREMENTS file AND PROJECT.md replace the blanket "Mayhem firmware control out of scope" statement with a bounded one that in-scopes only the single mode-switch command (D-00)
    - [ ] **REQ-P5-01**: Udev rule matches Portapack via `ATTRS{idVendor}` + `ATTRS{idProduct}` (VID:PID only) (D-01)
    - [ ] **REQ-P5-02**: Udev rule creates stable symlink `/dev/portapack` on the CDC-ACM interface via `SYMLINK+="portapack"` (D-02)
    - [ ] **REQ-P5-03**: docker-compose.yaml declares `device_cgroup_rules: ['c 189:* rmw', 'c 166:* rmw']` and bind-mounts `/dev:/dev` (D-03)
    - [ ] **REQ-P5-04**: Udev rule is installed on the host filesystem at `/etc/udev/rules.d/99-portapack.rules` via a deployment script (NOT baked into the container image) (D-04)
    - [ ] **REQ-P5-05**: Serial handshake opens `/dev/portapack` with pyserial at 115200 8N1 and writes `b'hackrf\n'` as the mode-switch command (D-05)
    - [ ] **REQ-P5-06**: Transition sequence is open→write(`hackrf\n`)→close→poll `pyhackrf2.HackRF.enumerate()` until a HackRF appears or timeout elapses→open HackRF (D-06)
    - [ ] **REQ-P5-07**: When `/dev/portapack` is absent or `portapack_enable_transition=False`, the helper returns SKIPPED and proceeds directly to `pyhackrf2.HackRF(...)` (D-07)
    - [ ] **REQ-P5-08**: When `serial.Serial(...)` raises on an existing symlink (stale symlink case), the helper logs ERROR and returns SKIPPED (fall-through, not FAILED) (D-08)
    - [ ] **REQ-P5-09**: When the first re-enumeration attempt times out, the helper resends the hackrf command and polls once more; if still absent, returns FAILED and `on_configure` returns `TransitionCallbackReturn.FAILURE` (D-09)
    - [ ] **REQ-P5-10**: `on_configure` retries `pyhackrf2.HackRF(...)` up to `portapack_open_retries` times with `PORTAPACK_OPEN_RETRY_DELAY_S` (0.25 s) spacing to absorb the USB kernel-claim race (D-10)
    - [ ] **REQ-P5-11**: `_diagnostics_callback` publishes the `last_portapack_transition` field with one of `skipped`, `succeeded`, `retried`, `failed`; state is written on every configure before returning (D-11)
    - [ ] **REQ-P5-12**: Four new ROS2 parameters declared: `portapack_serial_device` (str, default `/dev/portapack`), `portapack_enable_transition` (bool, default True), `portapack_reenum_timeout_s` (float, default 5.0), `portapack_open_retries` (int, default 3) (D-12)
    - [ ] **REQ-P5-13**: Defaults for those parameters exist as module constants at the top of `hackrf_lifecycle_node.py` (D-13)
    - [ ] **REQ-P5-14**: None of the portapack_* parameters are dynamic; they are read once in `on_configure` and a mid-run change has no hardware effect (D-14)
    - [ ] **REQ-P5-15**: The diagnostics field values are exactly `skipped`, `succeeded`, `retried`, `failed` (enum-driven; format guarantee for downstream autonomy observers) (D-15)
    - [ ] **REQ-P5-16**: `_transition_portapack()` is called in `on_configure` between `self._declare_parameters()` and `pyhackrf2.HackRF(...)`, returns a `PortapackTransitionResult` enum, and never raises (D-16, Pitfall 2)
    - [ ] **REQ-P5-A1**: Portapack CDC-ACM VID:PID verified against live hardware (assumed `1d50:6018`, confirmed via `udevadm info -a -n /dev/ttyACM*` during deployment) (A1)
    - [ ] **REQ-P5-A3**: Post-open DTR/RTS settle of at least 50 ms is empirically sufficient (first bytes after `serial.Serial(...)` open not dropped), or `PORTAPACK_DTR_SETTLE_S` is raised to 100 ms (A3)

    ```

    **B. Append traceability rows to the Traceability table** (inside the `## Traceability` section). Locate the existing last row ending with `HW-02 | Phase 4 | Advanced Signal Intelligence | Pending`. AFTER that row, append these 19 rows (preserve pipe alignment):
    ```
    | REQ-P5-00 | Phase 5 | Portapack Boot Transition | Pending |
    | REQ-P5-01 | Phase 5 | Portapack Boot Transition | Pending |
    | REQ-P5-02 | Phase 5 | Portapack Boot Transition | Pending |
    | REQ-P5-03 | Phase 5 | Portapack Boot Transition | Pending |
    | REQ-P5-04 | Phase 5 | Portapack Boot Transition | Pending |
    | REQ-P5-05 | Phase 5 | Portapack Boot Transition | Pending |
    | REQ-P5-06 | Phase 5 | Portapack Boot Transition | Pending |
    | REQ-P5-07 | Phase 5 | Portapack Boot Transition | Pending |
    | REQ-P5-08 | Phase 5 | Portapack Boot Transition | Pending |
    | REQ-P5-09 | Phase 5 | Portapack Boot Transition | Pending |
    | REQ-P5-10 | Phase 5 | Portapack Boot Transition | Pending |
    | REQ-P5-11 | Phase 5 | Portapack Boot Transition | Pending |
    | REQ-P5-12 | Phase 5 | Portapack Boot Transition | Pending |
    | REQ-P5-13 | Phase 5 | Portapack Boot Transition | Pending |
    | REQ-P5-14 | Phase 5 | Portapack Boot Transition | Pending |
    | REQ-P5-15 | Phase 5 | Portapack Boot Transition | Pending |
    | REQ-P5-16 | Phase 5 | Portapack Boot Transition | Pending |
    | REQ-P5-A1 | Phase 5 | Portapack Boot Transition | Pending |
    | REQ-P5-A3 | Phase 5 | Portapack Boot Transition | Pending |
    ```

    **C. Update the Coverage block** (currently at lines 127-137). Replace:
    ```markdown
    **Coverage:**
    - v1 requirements: 32 total
    - Mapped to phases: 32
    - Unmapped: 0

    **Phase distribution:**
    - Phase 1 (Sensor Foundations): 14 requirements (MSG-01..06, DET-01..06, PERF-01..02)
    - Phase 2 (Robot Autonomy Integration): 10 requirements (SWP-01..03, REC-01..04, MAP-01..03)
    - Phase 3 (Reliability): 3 requirements (REL-01..03)
    - Phase 4 (Advanced Signal Intelligence): 5 requirements (ADV-01..03, HW-01..02)
    ```
    with (32 + 19 = 51 total):
    ```markdown
    **Coverage:**
    - v1 requirements: 51 total (32 original + 19 Phase 5)
    - Mapped to phases: 51
    - Unmapped: 0

    **Phase distribution:**
    - Phase 1 (Sensor Foundations): 14 requirements (MSG-01..06, DET-01..06, PERF-01..02)
    - Phase 2 (Robot Autonomy Integration): 10 requirements (SWP-01..03, REC-01..04, MAP-01..03)
    - Phase 3 (Reliability): 3 requirements (REL-01..03)
    - Phase 4 (Advanced Signal Intelligence): 5 requirements (ADV-01..03, HW-01..02)
    - Phase 5 (Portapack Boot Transition): 19 requirements (REQ-P5-00..16, REQ-P5-A1, REQ-P5-A3)
    ```

    Update the final timestamp at the bottom of the file (line 140 currently `*Last updated: 2026-04-13 after roadmap creation — traceability finalized*`) to:
    ```
    *Last updated: 2026-04-18 after Phase 5 requirement mint (REQ-P5-00..16 + REQ-P5-A1, A3)*
    ```

    This implements REQ-P5-00 (the scope-reversal requirement that tracks its own implementation in this plan), mints the REQ-IDs that all other Phase 5 plans reference, and keeps the traceability table + coverage counts consistent.
  </action>
  <verify>
    <automated>grep -q "### Portapack Boot Transition (Phase 5)" .planning/REQUIREMENTS.md &amp;&amp; grep -q "REQ-P5-00" .planning/REQUIREMENTS.md &amp;&amp; grep -q "REQ-P5-16" .planning/REQUIREMENTS.md &amp;&amp; grep -q "REQ-P5-A1" .planning/REQUIREMENTS.md &amp;&amp; grep -q "REQ-P5-A3" .planning/REQUIREMENTS.md &amp;&amp; [ "$(grep -cE '^\| REQ-P5-[0-9A-Z]+ \| Phase 5' .planning/REQUIREMENTS.md)" -eq 19 ] &amp;&amp; grep -q "v1 requirements: 51 total" .planning/REQUIREMENTS.md &amp;&amp; grep -q "Phase 5 (Portapack Boot Transition): 19 requirements" .planning/REQUIREMENTS.md &amp;&amp; grep -qE "REQ-P5-0[0-9]" .planning/REQUIREMENTS.md &amp;&amp; grep -qE "REQ-P5-1[0-6]" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains section heading `### Portapack Boot Transition (Phase 5)`
    - Every ID `REQ-P5-00`, `REQ-P5-01`, ..., `REQ-P5-16`, `REQ-P5-A1`, `REQ-P5-A3` appears at least twice (once in the new section, once in the traceability table)
    - Traceability table contains exactly 19 rows matching regex `^\| REQ-P5-[0-9A-Z]+ \| Phase 5 \| Portapack Boot Transition \| Pending \|` (grep -c returns 19)
    - Coverage block reads `v1 requirements: 51 total` (32 + 19)
    - Coverage block lists `Phase 5 (Portapack Boot Transition): 19 requirements`
    - Each REQ-P5-NN description mentions the corresponding `(D-XX)` or `(A1)`/`(A3)` citation
    - Final timestamp reads `*Last updated: 2026-04-18 after Phase 5 requirement mint (REQ-P5-00..16 + REQ-P5-A1, A3)*`
    - File is still valid Markdown (no broken table syntax — verify with `python3 -c "[l for l in open('.planning/REQUIREMENTS.md') if l.strip().startswith('|')]"` and spot-check row widths)
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md contains the Phase 5 requirement block, 19 traceability rows, and an updated coverage summary. Every other Phase 5 plan's `requirements` frontmatter can resolve to a concrete REQ-P5-NN row.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planner-authored docs → downstream executors | Incorrect REQ-P5-NN → D-XX mapping would cause executors to implement the wrong acceptance criteria |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Repudiation | Traceability table edit | mitigate | Every REQ-P5-NN description cites its D-XX (or A1/A3) source; the Coverage block's counts are arithmetic (32 + 19 = 51) and grep-verifiable in acceptance criteria. Zero ambiguity for downstream plans. |
| T-05-03-02 | Tampering | PROJECT.md scope line | mitigate | Acceptance criteria explicitly assert (a) the OLD string is absent and (b) two specific NEW strings are present. A silent revert cannot pass both checks. |
</threat_model>

<verification>
- Automated greps verify the OLD scope strings are gone and the NEW scope strings are present
- Traceability row count is exactly 19 for Phase 5 (grep -c on the row-matching regex)
- Coverage totals add up (32 + 19 = 51)
- Every REQ-P5-NN description cites its D-XX source (or A1/A3 for the two assumptions)
</verification>

<success_criteria>
- PROJECT.md and REQUIREMENTS.md reflect the D-00 bounded reversal
- REQ-P5-00..16, REQ-P5-A1, REQ-P5-A3 minted and traceable
- Downstream Phase 5 plans (05-01, 05-02, 05-04) can successfully reference these IDs in their `requirements` frontmatter
- Coverage counts consistent
</success_criteria>

<output>
After completion, create `.planning/phases/05-portapack-boot-transition/05-03-SUMMARY.md` listing the final REQ-P5-NN → D-XX mapping as a table and the diff ranges of both planning docs.
</output>
