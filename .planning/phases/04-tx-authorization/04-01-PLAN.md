---
phase: 04-tx-authorization
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - hackrf_ros/tx_controller.py
  - test/test_tx_controller.py
autonomous: true
requirements:
  - TX-02
  - TX-03
  - TX-04
  - TX-05

must_haves:
  truths:
    - "A TX command to a restricted frequency is rejected with TXFreqBlockedError regardless of auth token"
    - "A TX command with no auth token (or expired/wrong token) is rejected with TXNotAuthorizedError"
    - "A TX command with antenna not confirmed is rejected with TXBlockedError"
    - "Frequency filter can be disabled only when BOTH tx_freq_filter_enabled=False AND hackrf:tx:freq_filter_override=disabled"
    - "Auth token is consumed atomically (Lua GETDEL fallback works on Redis 6.0.16)"
    - "tx_skip_antenna_check=True bypasses antenna confirmation for automated testing"
  artifacts:
    - path: "hackrf_ros/tx_controller.py"
      provides: "TXController class with all guard logic"
      exports:
        - "TXController"
        - "TXBlockedError"
        - "TXFreqBlockedError"
        - "TXNotAuthorizedError"
    - path: "test/test_tx_controller.py"
      provides: "Standalone unit tests with mocked Redis and pyhackrf2"
  key_links:
    - from: "TXController._consume_auth_token()"
      to: "redis.eval(_LUA_GETDEL)"
      via: "Lua fallback with ResponseError catch for native GETDEL"
      pattern: "_LUA_GETDEL"
    - from: "TXController._freq_filter_active()"
      to: "tx_freq_filter_enabled ROS2 param + hackrf:tx:freq_filter_override Redis key"
      via: "dual-disable: both must say disabled"
      pattern: "FREQ_OVERRIDE_KEY"
---

<objective>
Implement TXController as a standalone helper class in hackrf_ros/tx_controller.py.
This class enforces all TX safety guardrails before any hardware access:
antenna confirmation, frequency allowlist, and one-token-per-TX authorization.

Purpose: Isolate guard logic in a testable unit so wiring into HackRFNode (Plan 02)
is purely integration work with no untested business logic.
Output: tx_controller.py with TXController class + test_tx_controller.py with full coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tx-authorization/04-CONTEXT.md
@.planning/phases/04-tx-authorization/04-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From hackrf_ros/redis_bridge.py (module-level dispatch table pattern to follow):
```python
# Module-level dispatch table — TXController follows the same class structure
_COMMAND_HANDLERS = {
    'setfreq': lambda node, p: node._set_center_frequency(p['freq_hz']),
    ...
}
```

From hackrf_ros/hackrf_node.py (methods TXController will call):
```python
def _stop_rx_if_running(self) -> None: ...   # half-duplex: pause RX before TX
def _start_rx_if_stopped(self) -> None: ...  # half-duplex: resume RX after TX
# node._hackrf  — pyhackrf2.HackRF() instance
# node._device_lock  — threading.RLock()
```

From test/test_redis_bridge.py (test structure pattern):
```python
class _Logger:
    def info(self, msg): ...
    def warning(self, msg): ...
    def error(self, msg): ...
```

Existing test files: test/test_redis_bridge.py, test/test_mayhem_serial.py
(use these as structural references for the new test file)

pyhackrf2 TX API (source-verified, RESEARCH.md):
```python
hackrf.buffer = bytearray(int8_bytes)  # load IQ data before start_tx
hackrf.start_tx()                       # no callback argument; buffer-based
hackrf.stop_tx()                        # safe to call when not transmitting
hackrf.txvga_gain = 0                   # 0-47 dB, default MUST be 0
```
</interfaces>
</context>

<feature>
  <name>TXController — guard logic and pyhackrf2 TX dispatch</name>
  <files>hackrf_ros/tx_controller.py, test/test_tx_controller.py</files>
  <behavior>
    Test 1 (freq blocked): _is_freq_restricted(433_000_000) returns False (ISM, allowed)
    Test 2 (freq blocked): _is_freq_restricted(121_500_000) returns True (aviation 108-137 MHz)
    Test 3 (freq blocked): _is_freq_restricted(850_000_000) returns True (cellular 700-900 MHz)
    Test 4 (freq blocked): _is_freq_restricted(1_090_000_000) returns True (ATC Mode S)
    Test 5 (filter dual-disable): _freq_filter_active() returns True when ros2 param=True (Redis irrelevant)
    Test 6 (filter dual-disable): _freq_filter_active() returns True when ros2 param=False, Redis key absent
    Test 7 (filter dual-disable): _freq_filter_active() returns False when ros2 param=False AND Redis=b'disabled'
    Test 8 (auth token): _consume_auth_token(redis, 'abc') returns True when Lua returns b'abc'
    Test 9 (auth token): _consume_auth_token(redis, 'abc') returns False when Lua returns None
    Test 10 (auth token): _consume_auth_token(redis, 'abc') returns False when Lua returns b'xyz' (mismatch)
    Test 11 (auth token): Native GETDEL tried first; ResponseError falls back to Lua eval
    Test 12 (antenna): open() sets _antenna_confirmed=True when Redis key = b'1'
    Test 13 (antenna): open() sets _antenna_confirmed=False when Redis key absent
    Test 14 (antenna): open() sets _antenna_confirmed=True when tx_skip_antenna_check=True (logs warning)
    Test 15 (start_tx guards): start_tx raises TXBlockedError when _antenna_confirmed=False
    Test 16 (start_tx guards): start_tx raises TXFreqBlockedError when freq restricted and filter active
    Test 17 (start_tx guards): start_tx raises TXNotAuthorizedError when token not in Redis
    Test 18 (start_tx guards): freq check happens BEFORE token consumption (restricted freq must NOT consume token)
    Test 19 (start_tx success): start_tx calls node._stop_rx_if_running, sleeps 0.1s, sets hackrf.buffer, calls hackrf.start_tx()
    Test 20 (stop_tx): stop_tx calls hackrf.stop_tx(), sets _is_transmitting=False, calls node._start_rx_if_stopped()
    Test 21 (stop_tx): stop_tx is a no-op when _is_transmitting=False
    Test 22 (audit log): start_tx logs AUDIT message containing freq_hz and token hint
  </behavior>
  <implementation>
    Create hackrf_ros/tx_controller.py with:

    Module-level custom exceptions:
    - TXBlockedError(Exception): antenna not confirmed
    - TXFreqBlockedError(Exception): frequency on restricted band
    - TXNotAuthorizedError(Exception): no valid auth token

    Module-level Lua script constant _LUA_GETDEL (per RESEARCH.md Pattern 1).

    TXController class:
    - RESTRICTED_BANDS: list of (min_hz, max_hz) tuples per D-04 / RESEARCH.md exact values:
        (108_000_000, 137_000_000),   # Aviation VHF
        (150_000_000, 174_000_000),   # Emergency/public safety
        (406_000_000, 406_100_000),   # EPIRB distress
        (450_000_000, 470_000_000),   # Emergency/public safety UHF
        (700_000_000, 900_000_000),   # Cellular LTE
        (960_000_000, 1_215_000_000), # Aviation DME/TACAN
        (1_030_000_000, 1_030_000_001),  # ATC Mode C exact
        (1_090_000_000, 1_090_000_001),  # ATC Mode S exact
        (1_164_000_000, 1_215_000_000),  # GPS L5
        (1_559_000_000, 1_610_000_000),  # GPS L1/L2
        (1_700_000_000, 2_100_000_000),  # Cellular AWS/PCS
        (2_500_000_000, 2_700_000_000),  # Cellular Band 41
    - AUTH_KEY = 'hackrf:tx:auth'
    - ANTENNA_KEY = 'hackrf:tx:antenna_confirmed'
    - FREQ_OVERRIDE_KEY = 'hackrf:tx:freq_filter_override'
    - IQ_DATA_KEY = 'hackrf:tx:iq_data'

    __init__(self, node, redis_client, logger):
        Store references. Set _antenna_confirmed=False, _is_transmitting=False.
        _tx_lock = threading.Lock() (not RLock — TX cannot re-enter itself).

    open(self) -> None:
        Read antenna confirmation per D-07/D-08 (RESEARCH.md Pattern 4):
        1. If node.get_parameter('tx_skip_antenna_check').bool_value is True:
             _antenna_confirmed = True; log WARNING about bypass
        2. Else: check Redis key ANTENNA_KEY; set _antenna_confirmed accordingly

    stop(self) -> None:
        Alias for stop_tx(). Called from destroy_node — must be safe to call any time.

    start_tx(self, freq_hz: int, auth_token: str, iq_bytes: bytes, txvga_gain: int = 0) -> None:
        Guards in ORDER (per anti-pattern: check freq BEFORE consuming token):
        1. if not _antenna_confirmed: raise TXBlockedError
        2. if _freq_filter_active() and _is_freq_restricted(freq_hz): raise TXFreqBlockedError
        3. if not _consume_auth_token(freq_hz, auth_token): raise TXNotAuthorizedError
        4. Acquire _tx_lock:
           a. node._stop_rx_if_running()
           b. time.sleep(0.1)  # firmware settle per libhackrf #916
           c. hackrf = node._hackrf (read under _device_lock in caller, but TX uses node ref)
           d. with node._device_lock:
                hackrf.center_freq = freq_hz
                hackrf.txvga_gain = txvga_gain
                hackrf.buffer = bytearray(iq_bytes)
                hackrf.start_tx()
           e. _is_transmitting = True
           f. Log AUDIT: TX started freq={freq_hz} token_hint={auth_token[:8]}... iq_len={len(iq_bytes)}

    stop_tx(self) -> None:
        Per RESEARCH.md Pattern 3:
        with _tx_lock:
            if not _is_transmitting: return
            try: node._hackrf.stop_tx() except RuntimeError: pass
            _is_transmitting = False
            time.sleep(0.1)  # settle before RX resume
            node._start_rx_if_stopped()
            log info: TX stopped; RX resumed.

    _is_freq_restricted(self, freq_hz: int) -> bool:
        Per RESEARCH.md Pattern 5.

    _freq_filter_active(self) -> bool:
        Per RESEARCH.md Pattern 5 — dual-disable model (D-05).

    _consume_auth_token(self, expected_token: str) -> bool:
        Per RESEARCH.md Option 1 — try native getdel first, fall back to Lua.

    Create test/test_tx_controller.py:
    - _Logger stub (same pattern as test_redis_bridge.py)
    - _MockNode with _hackrf MagicMock, _device_lock threading.RLock(),
      _stop_rx_if_running=MagicMock(), _start_rx_if_stopped=MagicMock(),
      get_parameter returns configurable MagicMock
    - All 22 test cases above as individual test methods
    - All Redis calls mocked (no live Redis required)
    - All pyhackrf2 calls mocked

    Follow project conventions: 4-space indent, flake8-clean, descriptive test method names.
  </implementation>
</feature>

<verification>
Run tests from workspace root:

```bash
cd /home/user/dev_ws/hackrf_ros && python -m pytest test/test_tx_controller.py -v 2>&1 | tail -30
```

All 22+ tests must pass. No live Redis or HackRF hardware required.

Also verify flake8 compliance:
```bash
cd /home/user/dev_ws/hackrf_ros && python -m flake8 hackrf_ros/tx_controller.py --max-line-length=120
```
</verification>

<success_criteria>
- hackrf_ros/tx_controller.py exists with TXController class, 3 exception classes, _LUA_GETDEL constant
- test/test_tx_controller.py exists with minimum 22 test methods, all passing
- Frequency check happens before token consumption (verified by test 18)
- Lua GETDEL fallback confirmed working in tests (test 11)
- All restricted bands from D-04 present in RESTRICTED_BANDS
- flake8 passes on tx_controller.py
</success_criteria>

<output>
After completion, create `.planning/phases/04-tx-authorization/04-01-SUMMARY.md`
</output>
