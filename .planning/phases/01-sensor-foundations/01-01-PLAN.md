---
phase: 01-sensor-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hackrf_interfaces/msg/SpectrumStamped.msg
  - hackrf_interfaces/msg/RFDetection.msg
  - hackrf_interfaces/msg/RFDetectionArray.msg
  - hackrf_interfaces/msg/RFEnvironment.msg
  - hackrf_interfaces/CMakeLists.txt
autonomous: true
requirements:
  - MSG-01
  - MSG-03
  - MSG-04

must_haves:
  truths:
    - "colcon build --packages-select hackrf_interfaces succeeds with zero errors"
    - "ros2 interface show hackrf_interfaces/msg/SpectrumStamped prints all 7 fields including std_msgs/Header"
    - "ros2 interface show hackrf_interfaces/msg/RFDetection prints center_frequency_hz, bandwidth_hz, power_dbm, snr_db, classification, persistence_frames, detection_id"
    - "ros2 interface show hackrf_interfaces/msg/RFDetectionArray prints noise_floor_dbm, occupancy_pct, and RFDetection[] detections"
    - "ros2 interface show hackrf_interfaces/msg/RFEnvironment prints emitter_count, occupancy_pct, noise_floor_dbm, peak_power_dbm, peak_frequency_hz"
  artifacts:
    - path: "hackrf_interfaces/msg/SpectrumStamped.msg"
      provides: "Stamped PSD message type"
      contains: "std_msgs/Header header"
    - path: "hackrf_interfaces/msg/RFDetection.msg"
      provides: "Per-signal detection type"
      contains: "detection_id"
    - path: "hackrf_interfaces/msg/RFDetectionArray.msg"
      provides: "Array of detections with frame metadata"
      contains: "RFDetection[] detections"
    - path: "hackrf_interfaces/msg/RFEnvironment.msg"
      provides: "1 Hz environment summary type"
      contains: "emitter_count"
  key_links:
    - from: "hackrf_interfaces/CMakeLists.txt"
      to: "hackrf_interfaces/msg/*.msg"
      via: "rosidl_generate_interfaces()"
      pattern: "SpectrumStamped.msg"
---

<objective>
Define the four custom message types that form the public API for Phase 1. All
downstream plans (driver upgrade, CFAR node) depend on these generated Python
bindings — this plan has no dependencies and can run first.

Purpose: Any ROS2 node can import `hackrf_interfaces.msg.SpectrumStamped` after
this plan completes and `hackrf_interfaces` is built.

Output: Four .msg files and an updated CMakeLists.txt. Verified by successful
`colcon build` and `ros2 interface show` on each type.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-sensor-foundations/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md

<interfaces>
<!-- Existing CMakeLists.txt pattern to extend -->
From hackrf_interfaces/CMakeLists.txt (current):
```cmake
rosidl_generate_interfaces(${PROJECT_NAME}
  "srv/Sweep.srv"
  DEPENDENCIES std_msgs
)
```

<!-- Message field specifications from ARCHITECTURE.md and CONTEXT.md -->

SpectrumStamped.msg (per D-09, MSG-01):
  std_msgs/Header header       # stamp=time of last IQ chunk; frame_id=antenna frame
  float64 center_frequency_hz
  float64 sample_rate_hz
  float64 bin_width_hz         # sample_rate_hz / fft_size
  uint32 fft_size
  float32[] psd_db             # length=fft_size, FFT-shifted (DC at center)
  float32 noise_floor_db       # median of psd_db, computed per frame

RFDetection.msg (per D-04, MSG-03):
  float64 center_frequency_hz
  float64 bandwidth_hz
  float32 power_dbm            # peak power within detection bandwidth
  float32 snr_db               # peak above local noise estimate
  string classification        # e.g. "wifi_2_4g", "ble", "zigbee", "lte", "ism_900", "unknown"
  uint32 persistence_frames    # consecutive frames this detection has existed
  uint32 detection_id          # stable across frames via frequency-proximity matching

RFDetectionArray.msg (per D-05, MSG-03):
  std_msgs/Header header
  float64 noise_floor_dbm      # system noise floor from SpectrumStamped
  float32 occupancy_pct        # fraction of monitored band with detections above noise
  RFDetection[] detections

RFEnvironment.msg (per D-06, MSG-04):
  std_msgs/Header header
  uint32 emitter_count         # confirmed detections (persistence >= N_confirm)
  float32 occupancy_pct
  float32 noise_floor_dbm
  float32 peak_power_dbm
  float64 peak_frequency_hz
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create four message definition files</name>
  <files>
    hackrf_interfaces/msg/SpectrumStamped.msg
    hackrf_interfaces/msg/RFDetection.msg
    hackrf_interfaces/msg/RFDetectionArray.msg
    hackrf_interfaces/msg/RFEnvironment.msg
  </files>
  <read_first>
    - hackrf_interfaces/CMakeLists.txt (understand existing structure)
    - hackrf_interfaces/pkg.xml (confirm std_msgs is already a depend)
    - .planning/research/ARCHITECTURE.md (message field specs — Custom Message Design section)
    - .planning/phases/01-sensor-foundations/01-CONTEXT.md (D-04, D-05, D-06, D-09)
  </read_first>
  <action>
Create `hackrf_interfaces/msg/` directory. Write four .msg files with exact field names
as specified in the &lt;interfaces&gt; block above.

SpectrumStamped.msg:
```
std_msgs/Header header
float64 center_frequency_hz
float64 sample_rate_hz
float64 bin_width_hz
uint32 fft_size
float32[] psd_db
float32 noise_floor_db
```

RFDetection.msg:
```
float64 center_frequency_hz
float64 bandwidth_hz
float32 power_dbm
float32 snr_db
string classification
uint32 persistence_frames
uint32 detection_id
```

RFDetectionArray.msg:
```
std_msgs/Header header
float64 noise_floor_dbm
float32 occupancy_pct
hackrf_interfaces/RFDetection[] detections
```

RFEnvironment.msg:
```
std_msgs/Header header
uint32 emitter_count
float32 occupancy_pct
float32 noise_floor_dbm
float32 peak_power_dbm
float64 peak_frequency_hz
```

Note for RFDetectionArray.msg: use the fully qualified `hackrf_interfaces/RFDetection[]`
type so rosidl resolves the nested type within the same package.
  </action>
  <verify>
    <automated>ls /home/mvc/dev_ws/hackrf_ros/hackrf_interfaces/msg/ | sort</automated>
  </verify>
  <done>Four .msg files exist: RFDetection.msg, RFDetectionArray.msg, RFEnvironment.msg, SpectrumStamped.msg</done>
</task>

<task type="auto">
  <name>Task 2: Register messages in CMakeLists.txt and build</name>
  <files>
    hackrf_interfaces/CMakeLists.txt
  </files>
  <read_first>
    - hackrf_interfaces/CMakeLists.txt (current content — must preserve Sweep.srv)
    - hackrf_interfaces/msg/SpectrumStamped.msg (verify file created in Task 1)
  </read_first>
  <action>
Update `hackrf_interfaces/CMakeLists.txt` to register all four new .msg files alongside
the existing Sweep.srv. Replace the rosidl_generate_interfaces() call with:

```cmake
rosidl_generate_interfaces(${PROJECT_NAME}
  "msg/SpectrumStamped.msg"
  "msg/RFDetection.msg"
  "msg/RFDetectionArray.msg"
  "msg/RFEnvironment.msg"
  "srv/Sweep.srv"
  DEPENDENCIES std_msgs
)
```

Keep all other CMakeLists.txt content unchanged (cmake_minimum_required, project(),
find_package lines, ament_package()).

After editing, build and verify:

```bash
cd /home/mvc/dev_ws/hackrf_ros
colcon build --packages-select hackrf_interfaces
source install/setup.bash
ros2 interface show hackrf_interfaces/msg/SpectrumStamped
ros2 interface show hackrf_interfaces/msg/RFDetection
ros2 interface show hackrf_interfaces/msg/RFDetectionArray
ros2 interface show hackrf_interfaces/msg/RFEnvironment
```

If build fails, check: (a) `hackrf_interfaces/msg/` directory exists, (b) nested type
in RFDetectionArray uses `hackrf_interfaces/RFDetection[]` not just `RFDetection[]`,
(c) no trailing whitespace in .msg files that confuses rosidl.
  </action>
  <verify>
    <automated>cd /home/mvc/dev_ws/hackrf_ros && colcon build --packages-select hackrf_interfaces 2>&1 | tail -5 && source install/setup.bash && ros2 interface show hackrf_interfaces/msg/SpectrumStamped | grep "noise_floor_db"</automated>
  </verify>
  <done>
    - colcon build exits 0 with "1 package finished"
    - `ros2 interface show hackrf_interfaces/msg/SpectrumStamped` prints all 7 fields
    - `ros2 interface show hackrf_interfaces/msg/RFDetection` prints detection_id field
    - `ros2 interface show hackrf_interfaces/msg/RFDetectionArray` prints detections[]
    - `ros2 interface show hackrf_interfaces/msg/RFEnvironment` prints emitter_count
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| message schema → colcon build | .msg file content directly controls generated Python/C++ bindings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | msg field naming | accept | Field names are locked to CONTEXT.md decisions; no runtime attack surface on .msg files |
| T-01-02 | Denial | RFDetectionArray nested type | mitigate | Use fully-qualified `hackrf_interfaces/RFDetection[]` — ambiguous short form causes build failure |
</threat_model>

<verification>
After both tasks complete:

```bash
source /home/mvc/dev_ws/hackrf_ros/install/setup.bash
python3 -c "from hackrf_interfaces.msg import SpectrumStamped, RFDetection, RFDetectionArray, RFEnvironment; print('All imports OK')"
```

Expected output: `All imports OK`
</verification>

<success_criteria>
- Four .msg files committed in hackrf_interfaces/msg/
- CMakeLists.txt includes all four msg files in rosidl_generate_interfaces()
- `colcon build --packages-select hackrf_interfaces` exits 0
- Python import of all four message types succeeds after sourcing install/setup.bash
- grep "detection_id" hackrf_interfaces/msg/RFDetection.msg exits 0
- grep "std_msgs/Header" hackrf_interfaces/msg/SpectrumStamped.msg exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-sensor-foundations/01-01-SUMMARY.md`
</output>
